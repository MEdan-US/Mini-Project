Nhận xét:
- Decision Tree: Có kết quả kém nhất. Các chỉ số sai số (MAE, RMSE) đều cao nhất và độ chính xác (R2) thấp nhất. cao hơn một chút so với mô hình cơ sở.
- Linear Regression: Nó giải thích được tới 89.48% dữ liệu, sai số trung bình chỉ lệch 0.125 điểm.
- Random Forest cho kết quả vượt trội nhất. R-squared (R2) đạt 90.29%, sai lệch trung bình (MAE) thấp nhất chỉ vào khoảng 0.119 điểm GPA.
--> Mô hình tốt nhất là Random Forest để dự đoán điểm số chính xác nhất cho từng sinh viên cụ thể
